version: "2"
sql:
  - engine: "mysql"
    queries: "repository/query.sql"
    schema: "repository/model.sql"
    gen:
      go:
        package: "repository"
        out: "repository"
        emit_json_tags: true